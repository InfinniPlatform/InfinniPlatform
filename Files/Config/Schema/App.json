{
  "id": "https://raw.githubusercontent.com/InfinniPlatform/InfinniPlatform/master/Files/Config/Schema/App.json",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "App",
  "description": "Настройки приложения.",
  "type": "object",
  "properties": {
    "AppName": {
      "description": "Имя приложения. Используется для изоляции данных между приложениями.",
      "type": "string",
      "default": "InfinniPlatform"
    },
    "AppInstance": {
      "description": "Имя текущего экземпляра приложения. Используется для идентификации экземпляра приложения в кластере.",
      "type": "string"
    },
    "ServerName": {
      "description": "Адрес или имя сервера.",
      "type": "string",
      "default": "localhost"
    },
    "ServerPort": {
      "description": "Номер порта сервера.",
      "type": "integer",
      "default": 9900
    },
    "StaticFilesMapping": {
      "description": "Соответствие виртуальных и физических путей до статических файлов.",
      "type": "object",
      "properties": {},
      "default": {
        "/metadata": "content/metadata",
        "/": "content/www"
      }
    },
    "EmbeddedResourceMapping": {
      "description": "Соответствие виртуальных путей и сборок с файлами ресурсов.",
      "type": "object",
      "properties": {}
    }
  },
  "required": [
    "AppName"
  ]
}